# Not a source of truth — auth, storage, and API settings live in the Supabase
# dashboard. Never read values from this file or add settings mirroring the
# dashboard; `config push` is never run here and there is no local stack. Exists
# only so the CLI can resolve the project for `pnpm db:push` and `pnpm db:types`.

project_id = "seminova"
