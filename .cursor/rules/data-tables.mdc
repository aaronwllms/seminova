---
description: Conventions for shadcn/TanStack Table data tables across the app
globs:
  - 'src/components/**/*table*.tsx'
  - 'src/app/**/*table*.tsx'
---

# Data Table Conventions

## Search Pattern

Each data table gets a single search input, scoped to one explicitly-designated text column — never inferred from column position or implemented as a global multi-column search.

**Exception:** [`src/app/admin/logs/_components/logs-table.tsx`](../../src/app/admin/logs/_components/logs-table.tsx) OR-searches message, tag, and context by product spec (PRD Epic 9.2) — the purge window keeps the table small enough that multi-field search is intentional there.

- Designate the searchable column explicitly in the table's column config (e.g. a `searchable: true` flag or equivalent), not by assuming "first column."
- Filter via substring match on that one column.
- Non-text filters (booleans, enums, dates) are separate controls (dropdown, toggle, date picker) — never folded into the search box.

**Why:** global multi-column search is ambiguous to the user (unclear what matched) and doesn't generalize to non-string columns or server-side queries without expensive `OR ILIKE` scans across every column.

## Pagination Pattern

Every data table paginates server-side. Never load an unbounded result set.

- Controls: Next/Previous only — no numbered page strip. Avoids requiring a total-row-count query, and generalizes to data sources that don't cheaply expose totals.
- Page size: configurable via shared `DataTablePaginationControls` — options from [`src/constants/data-table.ts`](../../src/constants/data-table.ts) (`DATA_TABLE_PAGE_SIZE_OPTIONS`: 10 / 15 / 25 / 50; default `DATA_TABLE_DEFAULT_PAGE_SIZE`: 15).
- Layout: table grows naturally; the page scrolls. No fixed-height internal scroll region on the table body.

**Offset vs cursor paging.** Pick the strategy that matches the data source's churn and sort needs:

- **Offset / page-number paging** (`page` + `perPage`) — for low-churn, bounded sources that need flexible multi-column server sort. Drift and deep-page cost don't bite when the table isn't growing under the reader and totals are cheap enough. Production example: [`src/app/admin/users/_components/users-table.tsx`](../../src/app/admin/users/_components/users-table.tsx) via `admin_list_users` RPC.
- **Cursor paging** (keyed on a stable ordered column, typically with a tie-breaker id) — for high-insert or unbounded sources where offset paging drifts and slows under concurrent writes. Sort is limited to the cursor key (and its tie-breaker); other columns don't sort. Production example: [`src/app/admin/logs/_components/logs-table.tsx`](../../src/app/admin/logs/_components/logs-table.tsx) on `(created_at, id)`.

**Exception — fixture demos.** A demo table over a static, bounded fixture (e.g. the `/reference` page) may paginate client-side in memory, since there is no server to page against. This is the only sanctioned deviation from server-side pagination; production tables backed by a real data source always paginate server-side. Fixture demos still follow the search, page-size, and loading conventions above.

## Sorting Pattern

- **Production tables:** server-driven sort via `manualSorting` on `useDataTableShell` — sort params flow to the data layer (RPC, API, etc.); `getSortedRowModel()` does not re-sort the current page client-side.
- **Fixture demos:** may sort client-side over the full filtered fixture before slicing the page — same `manualSorting` shell API, sort logic lives in the demo hook (not `getSortedRowModel()`).

## Loading Pattern

While a fetch is in flight and the table body is empty, show skeleton rows — not placeholder text in `emptyMessage`.

- Use `DataTableShell` `isLoading` when loading and `table.getRowModel().rows.length === 0`.
- Skeleton rows render via `DataTableSkeletonBody` using the `Skeleton` primitive (`src/components/ui/skeleton.tsx`).
- Per-column skeleton shape via `columnDef.meta.skeletonClassName` (see users columns for reference) — shape only; width and alignment come from `cellClassName` (see [Column sizing](#column-sizing)).
- Default skeleton row count: 8 — intentionally fewer than page size (15). Enough to signal loading without flooding the viewport or implying a specific result count.
- When stale rows are already on screen (pagination/search refetch), keep showing stale data with light table dimming while `isFetching`; disable controls via `isPending` — do not flash skeleton over existing rows. Visual dim uses [`TableFetchDimWrapper`](../../src/components/table-fetch-dim-wrapper.tsx) with a minimum hold matching `duration-swept` so fast refetches complete a full fade cycle; `aria-busy` reflects actual fetch state only.
- `emptyMessage` is for the zero-results state only.

**Why:** placeholder text in a single merged cell reads as an empty state, not a loading state; skeleton rows preserve column structure and match the design-system loading convention.

## Column sizing

Per-column width and alignment via `columnDef.meta.cellClassName` — applied to both header and body cells by `DataTableShell` and `DataTableSkeletonBody`. Examples: `text-right` for right-aligned columns, `w-0 whitespace-nowrap` for icon/action columns that should shrink to content. Pair with `skeletonClassName` for loading-state shape (e.g. `inline-block` when alignment depends on the cell, not `ml-auto` on the skeleton).

## Reference Implementations
- [`src/components/data-table-shell.tsx`](src/components/data-table-shell.tsx) — `DataTableShell`, `useDataTableShell`, `DataTableColumnHeader`
- [`src/constants/data-table.ts`](../../src/constants/data-table.ts) — shared page-size options and default
- [`src/components/data-table-pagination-controls.tsx`](src/components/data-table-pagination-controls.tsx) — shared page-size selector + Previous/Next controls
- `src/app/admin/users/_components/users-table.tsx` — Users table, search on `email`, offset paging + server sort via `admin_list_users` RPC, skeleton loading via `DataTableShell`. Production reference for offset paging.
- `src/app/admin/logs/_components/logs-table.tsx` — Logs table, cursor paging on `(created_at, id)`, timestamp-only server sort, modal row detail via `onRowClick`, skeleton loading via `DataTableShell`. Production reference for cursor paging.
- `src/app/(marketing)/reference/_components/reference-table-section.tsx` — Fixture demo table; client-side sort + pagination over a bounded fixture (sanctioned exception per Pagination Pattern), composed like production list views (stat tiles, toolbar, filter chips, refresh, table shell, pagination).
