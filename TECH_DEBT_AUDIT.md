# Tech Debt Audit — Seminova

Last full audit: 2026-08-28
Last synced: 2026-08-29
Scope: Full repository pass — `src/**`, `scripts/**`, `eslint-rules/**`, `supabase/**`, root config, and doc-vs-reality reconciliation. 26.1k LOC non-test TypeScript, 17.2k LOC tests across 167 test files. This sync: re-verified all 17 Open rows in code — none closed. Citation drift only: F163 `proofCta.links` is now 40–43 (comment inserted above the array). Accepted F124 citation tightened to `listAllUsers` / `findUserByEmail` after the F150 file shrink. F185's full-scan `-- debt:` marker landed later the same day on the plus-one `admin_list_users` rewrite; finding stays Accepted. F053 gate still open ([vercel/next.js#89754](https://github.com/vercel/next.js/issues/89754), last Next-team comment 2026-08-04). Latest close same day: F118. Prior same-day: F152; F117; F163 / F171 / F173; F150 / F172 / F184; F145 / F164 / F165; F148 / F115 / F168; F151; F149 / F179; F176 / F178; F159 reviewed and moved to § Accepted without action; F116; F133 / F134 / F136; F188 / F190 / F166. Harvests F191–F195 remain Accepted. 9 Open remain.

## Executive summary

- **Latest close (same day): F118.** `useRefreshIndicator` in `src/hooks/` owns the in-flight counter, 1000ms hold, and unmount clear; logs / users / `/reference` call it; `REFRESH_MIN_VISIBLE_MS` lives next to the hook; `AdminRefreshButton` in `admin/_components/` is the toolbar icon-button on both admin toolbars. Filtered-empty-state Refresh text buttons and the `/reference` toolbar icon were deliberately left — different controls, under the extract threshold. F167 / F183 / F174 / F158 were not done here. 9 Open remain.
- **Prior same-day close: F152.** Shared `MIN_PASSWORD_LENGTH`; password submit awaits `referenceDemoMockPersist` so "Updating…" can appear; blob avatar URLs revoke on unmount; demo envelopes use `CONFLICT` / `INTERNAL_ERROR`; `app-toast.ts` owns info / warning / error / promise helpers and the toast section calls those; header comment lists only remaining intentional divergences (absent first-password branch). `reference/_components/**` stay coverage-excluded by design — the parity claim is what this closed, not the exclusion. F118 / F155 / F177 were not done here.
- **Prior same-day close: F117.** `useActiveAnchor` in `src/hooks/` owns the observer, sort, and disconnect; both section navs call it; `SECTION_SCROLL_CLASS` in `src/constants/section-scroll.ts` is the only `scroll-mt-24` name; the two page-prefixed aliases are gone. Shared pill markup and the `border-b` wrapper drift were deliberately left — two sites is under the extraction threshold, so no shared `SectionNav` component was extracted.
- **This sync (2026-08-29): closed nothing at the re-verify.** All 17 Open rows still matched the cited code then. F163 citation moved 39–42 → 40–43 (now closed). Accepted F124 citation tightened to `22:59` after F150 shrank `admin-users.ts`. F053 gate still open ([vercel/next.js#89754](https://github.com/vercel/next.js/issues/89754), last Next-team comment 2026-08-04). F185's full-scan `-- debt:` marker landed later the same day (comment-only on the plus-one rewrite; finding stays Accepted). No other new `// debt:` markers since F191–F195.
- **Prior same-day close: F150 / F172 / F184.** `use-mobile` has `'use client'` and reads `matches`; avatar-upload options use `RefObject`; `scripts/admin/lib/env.ts` is gone; `cli.ts` imports `loadServiceEnvForCli` from `@/utils/env`; `admin-users` re-exports only `deleteUserById`; `isUserAdmin` alias is gone. F174 / F185 were not done there.
- **Prior same-day sync closed nothing else in Open.** All 32 Open rows still matched the cited code before F176 / F178; later on 2026-08-29 one row left Open without a fix: F159 (`AppBanner` variant maps) was reviewed and moved to § Accepted — both the `cva` conversion and the `Alert` composition it recommended are net additions rather than reductions. Three undeclared `debt:` markers harvested as F193 (200ms accordion deferral left by F116), F194 (`px-2` on the password trigger), and F195 (`resize-none` on the bio field). F117 and F145 descriptions tightened. Top 5 refilled from the four remaining Open Mediums.
- **Prior same-day closures still hold in code.** F116 (native `scrollIntoView`, marker now F193); F133 / F134 / F136 (cursor-stack, data-table shell, `parseAppSettingValue` unit tests); F188 / F190 / F166 (homepage OG inherit, profile-dialog revalidate helper, security-audit migration count).
- **Auth-boundary enforcement gaps are closed (F187, S012).** The matcher now permits Next.js route-group hash suffixes on metadata images (5–6 base-36 chars at the image position only); `/auth/**` is a real path segment; the public-route predicate is exported and pinned against discovery; metadata-image files are location-checked. Residual: ≤4-character hash suffixes stay gated (~0.04% of route groups) — marked with a `// debt:` comment in `proxy-matcher.ts`.
- **Dependency findings all closed.** `next` was upgraded 16.2.9 → 16.3.3 (F101) and the stale override pins were refreshed (F102), taking `pnpm audit` from 20 vulnerabilities to zero. F081's `sharp` override was removed in the same commit as F102, its blocker cleared by the upgrade. Note both version targets recorded at this pass turned out stale or wrong — see § Resolved for the Active-LTS and capped-range lessons.
- **Three High correctness bugs closed this window (F103, F104, F107).** The client-log relay now strips caller-supplied `userId` before merge, so the server-read session is the only path to attribution. Proxy redirects copy the full cookie object (attributes included). Admin users pagination uses a `LIMIT per_page + 1` probe and slices the overflow row, matching logs. The human still needs to `pnpm db:push` the plus-one migration if it is not already on the linked project.
- **Three reopen triggers fired since the last pass are now closed.** F061 (duplicate profile dialog providers) had grown to four mount sites and is now closed by one `ProfileDialogHost` per kept shell with a sync host and async tree. F089 (next-themes hydration guards) fired at three identical copies and is now closed by the extracted `useMounted` hook. F060 resolved itself in the other direction — the second copy of the shared grid track dissolved in Phase 19, leaving only a stale comment (F139).
- **The enforcement layer is now gated by per-glob coverage floors.** All four custom lint rules now have `lintText` pins in `eslint.config.unit.test.ts`, and the helper resolves the rule from whichever plugin owns it (F108 closed). Coverage `include` now covers `scripts/**` and `eslint-rules/**` so the enforcement layer appears in the report (F122 closed); untested admin CLI shims and `vitest-file.mjs` are explicit commented excludes. Per-glob thresholds on `eslint-rules/**` and `scripts/**` in `vitest.config.ts` (F189 closed) fail `test:ci` when untested code grows in those paths even though the global 80% still passes. F119 closed: `motion-tier.mjs` and `semantic-tokens.mjs` now share one class-string visitor in `eslint-rules/lib/class-string-visitor.mjs`; the `eslint-rules/**` floor was re-baselined 2026-08-29 to 71 / 62 / 60 / 71 (lines / functions / branches / statements). F186 closed: `checks-wired.mjs` and `pnpm-only.mjs` now export testable comparison functions with colocated unit tests; residual uncovered lines in this layer are the `isMain` reporters (same as the other four checkers). F109 closed: the settings registry value-carrying cycle is gone — `madge --circular --ts-config` is clean; the no-`--ts-config` invocation remains unreliable because it resolves no `@/*` aliases.
- **Admin authorization contract at group level.** F105 closed: `assertAdminCaller` and `AdminActionError` now live in `admin/_lib/` alongside `map-admin-action-fault`; the logs shim and upward imports from the users surface are gone. F128 closed: `listUsersAction` now `safeParse`s at the trust boundary with a 200-character cap on `emailFilter`. F149 and F179 closed: mutation middle layer collapsed — two action files call `runAdminUserMutation` directly; one result type per family; `AdminUserTargetInput` is the shared user-id input. Server-side authorization itself is sound — see § Verified OK.
- **Quality gates are green** — `type-check`, `lint`, and `test:ci` all pass; 868 tests / 167 files; coverage thresholds met. Declared-marker hygiene from this window is closed (F141–F143); remaining markers are ceiling-gated in Accepted (F062, F100, F144, F191–F195) except F053 (Open/Deferred, external Next issue). The gates are not what is failing here; the gaps are in what the gates cover.
- **Volume caveat:** this pass produced 79 findings against a 30–80 target. That is the cost of 13 months of phase work with only sync passes in between, not a sudden collapse in quality — 44 of the 79 are Low severity and most are single-file, S-effort.

## Architectural mental model

Seminova is a **Next.js 16 App Router template** organized by route group: public `(marketing)/`, `auth/`, authenticated `(app)/` at `/home`, and `admin/` at `/admin/**`. Session gating runs in `src/proxy.ts` → `src/supabase/proxy.ts` — the single highest-churn source file in the repo at 21 commits in six months. The **two-authority refresh model** (proxy on matched server requests plus browser foreground auto-refresh) per ADR-0005 is what makes Supabase Realtime viable on `/admin/logs`.

Data access splits three ways: a browser client under RLS, a server session client, and a secret-key service client reserved for admin user mutations, log persistence, and first-password setup. The **admin surface is the complexity center** — three sibling surfaces (users, logs, settings) that solve overlapping problems (pagination, filter chips, refresh indication, save orchestration) and have drifted apart from each other in the details. Phase 14 extracted the shared helpers that were obviously shared; the paging and search-key bugs (F107, F130) are closed. What remains is the harder class, where two surfaces still solve the same problem *differently* — but the minimum-visible refresh indicator now lives in `useRefreshIndicator` (`src/hooks/`), shared by logs, users, and the `/reference` demo (F118 closed).

A second, quieter axis of the architecture is the **enforcement layer**: nine hard constraints, each backed by a check script or custom ESLint rule, wired into both `pre-push` and CI. This layer is load-bearing for the template's value proposition — a spinoff inherits the guardrails, not just the code. All six check scripts now have colocated unit tests; residual uncovered lines are the CLI entry reporters (`isMain` blocks), admin CLI entry shims, and `vitest-file.mjs`.

**Hot paths:** `src/proxy.ts` (every matched request), `require-auth.ts`, the two admin table-state hooks and their TanStack Query layers, `persistAppLogRow`, profile blur-save, avatar upload.

**Cold corners:** strict nonce-based `script-src` (F053, externally blocked), MSW infrastructure (F072, accepted), rate limiting on `/api/client-logs` (F082).

**Largest source files (LOC, non-test):** `banner-setting-row.tsx` (492), `workflow-diagram.tsx` (486), `use-admin-users-table-state.ts` (385), `use-admin-logs-table-state.ts` (375), `profile/actions.ts` (352), `a11y-structure.mjs` (350), `reference-profile-settings-preview.tsx` (346), `features-content.ts` (332). All re-verified against ADR-0001; the first four are legitimate deep modules (see § Verified OK).

**Git churn (6 months):** `supabase/proxy.ts` (21), `logs-table.tsx` (19), `login-form.tsx` (18), `users-table.tsx` (18), `sign-up-form.tsx` (16), `config/site.ts` (14), `workflow-diagram.tsx` (14). Auth-form correctness (F114) and users list paging/search (F107, F130) closed this window. The logs cursor-stack hook (F133) now has colocated unit coverage for its pagination transitions.

## Open

Actionable backlog only. `Status`: `Do next` | `Deferred` | `Needs decision`.

**Rows are not grouped by status.** The 2026-08-29 gate-triage pass flipped 22 rows in place rather than reordering the table; sort or filter on the Status column.

**Hygiene batch H1** closed 2026-08-29 (F163, F171, F173). Remaining throwaway-page Open Do-next: none. F167 / F183 stay Deferred with the next `/workflow` content change.

| ID | Status | Category | File:Line | Severity | Description | Recommendation | Effort |
| ---- | -------- | -------- | --------- | -------- | ----------- | -------------- | ------ |
| F053 | Deferred | Declared debt | `1:1:src/utils/security-headers.ts` | Medium | Verified unchanged and still accurate. Enforced CSP uses a deliberately permissive `script-src` (`'unsafe-inline'`); strict nonce-based `script-src` is incompatible with `cacheComponents` ([vercel/next.js#89754](https://github.com/vercel/next.js/issues/89754)). A second marker was added since the last pass declaring the development-only `'unsafe-eval'` allowance. Both triggers are external. `frame-ancestors 'none'` and `object-src 'none'` are present. | Revisit when #89754 closes. Home: future security phase, with F095. The W6 sentence that used to contradict the dev-eval marker is closed. Gate re-verified 2026-08-29 (later sync): [vercel/next.js#89754](https://github.com/vercel/next.js/issues/89754) is still open; last Next-team comment 2026-08-04, still assigned. | L |
| F095 | Deferred | Security hygiene | `32:32:src/utils/security-headers.ts` | Low | `style-src 'unsafe-inline'` remains required for Tailwind; styles are enforced with this exception and it carries no separate `// debt:` marker. Pairs with F053. | Revisit when strict `script-src` becomes possible; may need a nonce or hash strategy for styles too. Home: same security phase as F053. | L |
| F082 | Deferred | Security hygiene | `7:7:docs/adr/ADR-0007-client-log-relay-unauthenticated.md` | Low | The client log relay intentionally ships without rate limiting — a public POST surface bounded only by a closed registry and a same-origin check. Unchanged. F103 (forged `userId`) is closed; the remaining exposure is unrated row volume, not forged attribution. | Add a path-based rate limit (CDN or middleware) before production scale; the ADR documents the seam. Home: pre-production gate. | M |
| F123 | Deferred | Performance | `8:22:supabase/migrations/20260717234520_create_app_logs.sql` | Medium | `app_logs` carries only a btree index on `(created_at desc, id desc)` plus a partial unread variant. The admin log search issues a three-way leading-wildcard `ilike` across `message`, `tag`, and the generated `context_text` column, which no btree index can serve — so every search is a full scan of the retention window. `context_text` was added specifically as a search surface and never got a matching index. | Add a migration enabling `pg_trgm` with GIN trigram indexes on `message` and `context_text`; `tag` is low-cardinality and can stay on the existing scan. Home: whenever log volume makes search latency noticeable, or the next migration-carrying phase. | M |
| F125 | Deferred | Performance | `187:193:src/supabase/proxy.ts` | Medium | Every authenticated request to a protected route emits a debug log from the proxy. Because `appLog` resolves the threshold and persists inside `after()` this is off the response path — but if an admin sets `min_log_level` to `debug` in production, every request including RSC prefetches writes an `app_logs` row through the service client. Write volume then scales with traffic, bounded only by the daily retention purge. | Gate the per-request reuse/refresh debug line on `authCookiesUpdated` so it fires only when a token actually rotated, keeping the diagnostic value without the per-request row. Home: pre-production gate, with F082. | M |
| F167 | Deferred | Documentation drift | `224:279:src/app/(marketing)/workflow/_lib/workflow-page-content.ts` | Low | The nine CI hard constraints are restated here as marketing copy — a third copy alongside AGENTS.md's authoritative list and the check scripts themselves. Nothing keeps them in sync, so a constraint added or reworded in AGENTS.md leaves this page quietly wrong, on a public page whose whole promise is "this is what you inherit." | Derive the check names from the `package.json` `check:*` script keys (the real inventory) so a missing or renamed check is visible, and keep the human descriptions local. Home (recorded 2026-08-29): the next `/workflow` content change — concretely BACKLOG's "Workflow diagram — brief step" entry when promoted, which already commits to editing the documents table and the diagram. Bundle with F183. | S |
| F174 | Deferred | Consistency rot | `337:338:scripts/checks/a11y-structure.mjs` | Low | Entry-point detection has drifted across the check scripts. Four now guard against a missing `process.argv[1]` (`a11y-structure.mjs`, `a11y-contrast.mjs`, `checks-wired.mjs`, `pnpm-only.mjs`); `seo-base-url.mjs:94`, `no-profiles-role.mjs:53`, and `scripts/build/supabase-env.mjs:39` still call `pathToFileURL` on it unguarded and will throw a TypeError rather than report a violation if imported in a context without argv. | Extract a shared `is-main` helper; its home is an open question now that one consumer lives outside `scripts/checks/` (not settled at `scripts/checks/lib/`). Use it in all six check scripts plus the build guard — four already carry the guarded form; remaining work is `seo-base-url`, `no-profiles-role`, and the build guard. Home (recorded 2026-08-29): the next phase touching `scripts/checks/` — the same gate BACKLOG's "Opt-in EXECUTE for new database functions" entry names. | S |
| F181 | Deferred | Type & contract debt | `15:20:src/app/api/client-logs/_lib/client-log-relay-schema.ts` | Low | The relay `message` field is an unbounded string at the schema boundary — the length cap is applied afterward by the truncation helper. So the trust boundary accepts an arbitrarily large body and only bounds it after parsing, on a deliberately unauthenticated public endpoint with no rate limit by design (F082). The context object has the same shape of deferred bounding. | Add a `max()` on `message` matching the existing cap constant so oversized payloads are rejected at parse rather than silently trimmed. Low urgency — ADR-0007 accepts unbounded row count as a deployment concern and this is the same class of exposure. Home (recorded 2026-08-29): pre-production gate, with F082 and F125. Note the gate costs more than the work — this is one `max()` on a zod schema. | S |
| F183 | Deferred | Type & contract debt | `181:188:src/app/(marketing)/workflow/_components/workflow-diagram.tsx` | Low | The delegated mouseover handler reads a `data-` attribute off the closest matching DOM element and casts the result straight to the node-id union with no membership check. The attribute is set from the same constant array so it holds today, but the type system is being told something the runtime does not verify — on the highest-churn file in the marketing scope (14 commits in six months). | Guard with a membership check against the node array before setting state, and drop the cast; the lookup a few lines later already tolerates a miss. Home (recorded 2026-08-29): same trigger as F167 — the next `/workflow` change. | S |

## Accepted

Deliberately not doing now. Not a todo list.

| ID | Category | File:Line | Severity | Description | Why accepted | Reopen when | Effort |
| ---- | -------- | --------- | -------- | ----------- | ------------ | ----------- | ------ |
| F022 | Consistency rot | `19:29:src/components/login-form.tsx` | Low | Auth forms use `useState`; profile uses react-hook-form + zod. Two form stacks. | Intentional per `forms.mdc`. Re-verified this pass: the split itself is still the documented choice. F114 (FormData / min-length gaps inside the `useState` forms) is closed and did not require reopening this. | A product decision to unify form stacks. | — |
| F062 | Declared debt | `266:295:src/app/admin/settings/_components/app-setting-row.tsx` | Low | Two-type `log_level` / `positive_int` dispatch for logging settings; banner rows bypass this component entirely. F129 landed the `never`-typed exhaustiveness guard here; the `// debt:` marker is gone and the compiler is the gate. | Ceiling-gated — no third non-banner `valueType` exists. Verified this pass after F129. | A third non-banner `valueType` is added. | S |
| F071 | Architectural decay | `1:257:src/components/ui/dropdown-menu.tsx` | Low | Radix re-export barrel at ~257 LOC with many re-exported sub-primitives. | shadcn ecosystem surface; splitting fights upstream updates. Confirms the primitive-first hard constraint rather than violating it. | Import-tree bloat becomes measurable, or upstream splits the barrel. | — |
| F072 | Dependency & config | `95:95:package.json` | Low | MSW v2 devDependency and generated service worker present; zero handlers, no test imports. `knip` still flags `msw` unused. | PM decision recorded last pass: keep the scaffolding until the first HTTP integration test needs it. | The first HTTP integration test needs MSW handlers, or you decide to remove the scaffolding. | S |
| F080 | Performance | `1:486:src/app/(marketing)/workflow/_components/workflow-diagram.tsx` | Low | Large client component with a hover/focus state machine, no code-splitting; loads with the workflow page bundle. | Accepted for a marketing page; the diagram is deliberately in the coverage denominator. Re-verified as a deep module (see § Verified OK). | Bundle analyzer shows a workflow-page regression. | — |
| F091 | Error handling | `44:52:src/utils/persist-app-log.ts` | Low | Persist failures log via raw `console.error` and silent-drop. | Anti-recursion design; raw `console.error` is the documented exempt surface per `logging.mdc` and is in the ESLint exemption list. | Logs become production-critical and need metrics or alerts on the persist-failure rate. | — |
| F100 | Declared debt | `1:3:scripts/checks/a11y-structure.mjs` | Low | Five markers across the two a11y checkers declare static-analysis ceilings: the regex import closure misses barrel re-exports, dynamic imports, and multiline imports; dynamic `alt={expr}` is treated as satisfied; the heading-order walk skips segment layouts; OKLCH alpha is parsed but not composited; `var()` indirection is rejected rather than resolved. | Verified this pass: no concrete violation currently escapes these ceilings — the token file uses literal OKLCH throughout and no route imports through a barrel. | A real a11y miss escapes CI — then upgrade the checkers rather than papering over it with more rules. F137 is a genuine gap but outside these checkers' declared scope (landmarks), not a ceiling miss. | M |
| F124 | Performance | `22:59:scripts/admin/lib/admin-users.ts` | Medium | `findUserByEmail` pages through the entire `auth.users` table a thousand rows at a time and filters in JavaScript. Every CLI operation — promote, demote, delete — pays a full user-table download to locate one address, and `promoteUser` then fetches the same user again by id. Citation tightened this sync after F150 shrank the file (`listAllUsers` 22–47, `findUserByEmail` 49–59). | Moved from Deferred 2026-08-29: the trigger can only fire in a downstream fork, never in this repo, so `Deferred` implied a wait that will not end here. Same family as F185. The fix is known and cheap when it is needed — call the admin-gated `admin_list_users` RPC with escaped email search via the service client, keeping the paged listing only for `list-admins`. | A fork's user count makes the CLI slow. | M |
| F144 | Declared debt | `27:31:src/app/(marketing)/features/_components/feature-capability-card.tsx` | Low | Four paired markers describing the same static-analysis blind spot: `local/motion-tier` does not resolve identifiers passed to `cn()` back to their declaration, so removing `duration-swept` from a hoisted class constant would not be caught. Sites: this file (`// debt:`) and `landing-features.tsx:37` (`{/* debt:` JSX form — the harvest regex misses it), `site-footer.tsx:19` and `site-nav-links.tsx:15` (nav/footer link constants), and `src/app/admin/page.tsx` (admin dashboard card class constant — F143 hoist, still no marker on the constant). | Ceiling-gated and honestly declared. Re-verified this sync: no drift — the CTA copies are still byte-identical apart from a leading margin utility, and both nav constants still carry `duration-swept`. | Any pair actually drifts, or the lint rule is extended to scan top-level string constants (the stated upgrade path). F143's hoist added the fifth listed site. | S |
| F158 | Architectural decay | `1:31:src/components/public-banner-slot.tsx` | Low | Four-file copy-paste family: `public-banner-slot.tsx` and `authenticated-banner-slot.tsx` are structurally identical apart from the cookie constant; their loaders are identical apart from the cookie constant, the settings key, and one `await connection()`. | Two instances is under the three-site threshold in `code-minimalism.mdc`, and the parameterization would need to thread cookie name plus settings key plus the connection call. | A third banner surface appears, or the pair drifts again. | M |
| F159 | Architectural decay | `25:56:src/components/app-banner.tsx` | Medium | Four hand-rolled lookup tables keyed by `BannerVariant` carry the variant styling, and the first duplicates the owned `Alert` primitive's tint formula — `ui/alert.tsx:6-26` already defines `info`, `success`, `warning`, and `destructive` with the same `border-*/30 bg-*/15` shape. `StatTile` uses `cva` for the same job, so three mechanisms for variant styling coexist. | Reviewed 2026-08-29 and closed without action. The `cva` conversion the recommendation named is a net addition, not a reduction: the tints live on the `variant × part` cross-product, so every combination becomes a compound variant — five entries in each of three flat `Record`s (~24 lines, direct key lookup) become an empty `variant` axis plus fifteen compound-variant objects. `StatTile` is not the precedent it appeared to be: its `role` axis carries the real classes and its compound variants layer a border override on top, and it has no `part` dimension at all. Composition onto `Alert` — the finding's own better end state — does not hold either. `Alert`'s base is `rounded-lg border px-4 py-3` on a `grid-cols-[0_1fr]` track; `AppBanner` is a full-width `border-b` bar on a three-column track with its own badge and dismiss slots. Composing would override nearly all of the primitive's base and inherit a variant axis it then fights. What is genuinely shared is the `border-{token}/30 bg-{token}/15` **formula** across two files, five strings each — under the three-site threshold in `code-minimalism.mdc`, and a shared tint helper was already rejected. The current `Record`s are the right shape: one responsibility, five short strings readable at a glance. | A third surface hand-writes the same `border-*/30 bg-*/15` tint formula (three-site threshold met), or `AppBanner`'s layout converges with `Alert`'s enough that composition is a reduction rather than an override stack. | M |
| F175 | Consistency rot | `7:11:supabase/migrations/20260720151459_revoke_client_execute_trigger_functions.sql` | Low | Uppercase SQL with `SET search_path TO 'pg_catalog'` against the project convention of lowercase SQL and `set search_path = ''` with fully qualified names, and no header comment block per `supabase-sql.mdc`. The purge-function revoke half is closed by `20260828141556`. | Moved from Deferred 2026-08-29: applied migrations must not be rewritten, so the only path is a future redefinition that may never happen. `Deferred` overstated the likelihood. | `rls_auto_enable` is redefined — normalize in that migration. If the original was captured verbatim from the live database, say so in a comment instead. | S |
| F185 | Performance | `67:76:supabase/migrations/20260828193941_admin_list_users_per_page_plus_one_probe.sql` | Low | The live `admin_list_users` (rewritten by the F107 plus-one migration) still selects all of `auth.users` with no predicate and computes three derived booleans per row before the outer filter applies; the sort clause is a `CASE` stack no index can satisfy; pagination uses `OFFSET`. `admin_user_stats` performs the same unbounded scan. Every admin users page view is two full scans of the user table. | Not worth restructuring at template scale. Full-scan `-- debt:` marker landed 2026-08-29 next to the existing subquery-duplication marker; comment-only on the applied plus-one rewrite (not re-pushed). | A fork exceeds a few thousand users. Then push filters into the inner query, move to keyset pagination, and materialize the stats. | L |
| F191 | Declared debt | `36:36:vitest.config.ts` | Low | Coverage exclude on `src/app/**/opengraph-image.tsx` is marked ceiling-gated: thin OG segment files, cover via og-image util tests if threshold pressure returns. Harvested this sync — the marker was not in the tables. | Trigger has not fired — global 80% still holds without these files. | Coverage threshold pressure returns, or a segment file grows real logic. | — |
| F192 | Declared debt | `13:14:src/utils/proxy-matcher.ts` | Low | Matcher permits 5–6 character route-group hash suffixes only; ≤4-character suffixes (~0.04% of route groups) stay gated because they collide with words like `evil`. Residual of closed F187; the marker lived in the exec summary only until this sync. | Ceiling-gated; Next's `slice(0, 6)` without padding has not changed. | Next changes metadata-route hash width. Then widen to `{4,6}`. | — |
| F193 | Declared debt | `6:7:src/hooks/use-password-accordion-scroll.ts` | Low | 200ms `setTimeout` before `scrollIntoView` is marked to match `--animate-accordion-down`; a synchronous call lands short of the expanded layout. Residual of closed F116 — the marker shipped with the native-scroll replacement and was not in the tables. | Ceiling-gated; no post-layout hook exists yet. | A post-layout / animation-end hook replaces the timeout. Then drop the delay and the marker. | S |
| F194 | Declared debt | `89:90:src/app/(app)/_components/profile/profile-modal-content.tsx` | Low | JSX-form `{/* debt: */}` — extra `px-2` on the password accordion trigger breaks the modal content column; drop it when the trigger is restyled. Harvest regex (`// debt:`) misses this form, same class as F144's `landing-features.tsx` site. | Ceiling-gated; trigger restyle has not happened. | The accordion trigger is restyled and no longer needs the extra horizontal padding. | S |
| F195 | Declared debt | `160:160:src/app/(app)/_components/profile/profile-settings-form.tsx` | Low | JSX-form `{/* debt: */}` — `resize-none` is applied at the bio field call site (or should move onto the textarea primitive) because the native resize grabber fights the modal layout. Same harvest-regex miss as F194. | Ceiling-gated; neither the primitive nor this call site has been decided. | `resize-none` lands on the textarea primitive, or this call site is restyled so the grabber no longer fights the column. | S |

## Top 5

None — remaining Open rows are all Deferred.

## Quick wins

Low effort, Medium-or-higher severity, Open only. None this pass — every remaining Medium is M effort. H1 (F163 / F171 / F173) closed 2026-08-29.

## Verified OK

Areas that look alarming but are sound. Not a substitute for § Accepted.

- **Server-side admin authorization.** Every privileged action independently calls `assertAdminCaller` before touching data — the proxy is never treated as sufficient. Verified across all list, stats, tags, mark-read, and mutation actions. `assertAdminCaller` itself reads `app_metadata.role` via `getUser()` (a verified server call, not a client claim), fails closed on error, and never consults `profiles`. F105 (group-level home for this contract) is closed.
- **Self-target guards.** Ban and demote both reject the caller acting on their own account inside the action, not just in the UI.
- **Secret-key containment.** No client-reachable module imports `createServiceClient`. The ESLint default-deny plus explicit server allowlist covers `src/**` mechanically; `scripts/admin/lib/cli.ts` imports it under plain Node outside that deny, which is why `server-only` is not on the module.
- **ILIKE injection.** The logs search escapes backslash, `%`, and `_`, then wraps values containing commas or parens for PostgREST. Correct and non-obvious.
- **Realtime cleanup and scoping.** The logs channel is removed and the debounce timer cleared on both the disabled branch and unmount. Logs subscribes to INSERT only; users uses focus-refetch with no subscription — matching ADR-0008 exactly.
- **RLS and SQL hygiene.** All three custom tables have RLS with per-operation policies, `(select auth.uid())` wrapping, and no `for all`. Every `SECURITY DEFINER` function pins `search_path`. Column-level `grant update` on `profiles` restricts clients to display name, avatar, and bio, so `has_password` cannot be self-set. The migrations redefining `admin_list_users` in full (now six, including the F107 plus-one probe) are how immutable migration history works, not copy-paste duplication.
- **Deep modules, re-verified against ADR-0001.** `banner-setting-row.tsx` at 492 LOC (grown from 429 via Phase 18 persistence/dismissal, all serving one responsibility: edit one banner setting; four props, one real export, no co-change with siblings). `workflow-diagram.tsx` at 486 (one responsibility, one-prop interface, length is SVG coordinate markup). `use-admin-users-table-state.ts` (385) and `use-admin-logs-table-state.ts` (375) — wide *return* shapes, but each is a private channel to a single consumer, not a public API. `src/supabase/proxy.ts` at 189 (one responsibility behind one exported function, with Supabase-mandated ordering comments explaining why the internals cannot be split). `profile/actions.ts` at 352 (three server actions on one surface, length is repeated error-envelope literals, not tangled concerns).
- **Vendored shadcn surface.** knip's unused-export list is dominated by `src/components/ui/sidebar/**`, `table.tsx`, `tabs.tsx`, `toggle.tsx`, and `dropdown-menu.tsx`. This is the primitive-first hard constraint working as designed — composition surface area, not dead product code.
- **Dev-only `'unsafe-eval'` in the CSP.** `next.config.ts` evaluates `headers()` at build time, so the development branch cannot reach a production response. Correctly gated. The W6 sentence that used to contradict this is closed.
- **`src/supabase/server.ts` swallowing cookie-write errors.** Required by the RSC model and documented in the file header; the proxy is the writing authority.
- **`isSafeRedirect` and `isSafeUrlScheme` as two separate utilities.** Genuinely different threat models — same-origin `next` validation versus a render-time scheme check on DB-sourced URLs — and each file says so.
- **`getCurrentUserProfile` failing closed to `hasPassword: true`.** Looks like a wrong default; it is the deliberate fail-closed guard ADR-0012 requires so a failed profile load never exposes the no-reauth form.
- **`dangerouslySetInnerHTML` on the landing page.** The single sanctioned exception in `seo.mdc`; the JSON-LD is unicode-escaped by its builder before it arrives.
- **Env var discipline.** `env.ts` is the single reader of the Supabase vars and `site-url.ts` the single reader of the site-URL vars. Every `process.env` reference cross-checks against `.env.example` and README with no undocumented or unused variable.
- **`useToggleFilterSet` returning a memoized object.** Looks like speculative memoization; the object is consumed by downstream effect dependency arrays, so the stable identity is load-bearing.
- **`stat-tile.tsx` sr-only span alongside a Radix tooltip.** Looks like the mixed-ARIA-relationship antipattern; Radix removes tooltip content from the DOM when closed, so the span is the standard workaround, and three tests pin it.
- **Coverage exclusions for `page.tsx` / `layout.tsx` / `src/components/ui/**` / reference demo shells / static workflow sections / admin CLI entry shims / thin admin lib pass-throughs / `vitest-file.mjs`.** Thin shells, vendored primitives, static marketing prose, one-line CLI shims whose logic lives in tested `lib/` modules, and the `vitest-file.mjs` spawn wrapper. The interactive `workflow-diagram.tsx` is measured; so are all six check scripts (logic in exported functions; `isMain` reporters uncovered), `scripts/build/supabase-env.mjs`, `scripts/admin/lib/cli.ts` and `admin-users.ts`, and all four `eslint-rules/*.mjs` (via `lintText` pins). Note the reference exclusion is why F152's parity drift went unnoticed — the exclusion is defensible, the unverified parity *claim* is the finding.
- **No `src/services/` layer.** Two tables plus RPCs; premature per `code-minimalism.mdc`.
- **Dual admin gating (proxy + `AdminAuthGate`).** Defense-in-depth; `AdminAuthGate` probes `hasServerAuthSession` before reading claims on a cold cache. Keep both.
- **Zero `any`, `as any`, `as unknown as`, or `@ts-ignore` in production code; zero raw hex or numeric Tailwind color scales anywhere in `src/`; zero raw `console.*` outside the documented exempt surfaces.** The type and token discipline genuinely holds — the type findings in this audit are all about assertions and unsound generics, not escape hatches.

## Open questions

- **`AdminAuthGate` indefinite skeleton** — for an authenticated-but-not-yet-established session the gate renders a skeleton rather than redirecting. Is that a deliberate choice to avoid a redirect loop against the proxy, and is there a path where a user sits on it indefinitely instead of being sent to login?
- **`syncAdminSettingsVisitKey` mutating module state during render** — it is idempotent per pathname so StrictMode double-render is safe, but the doc comment claims it is "safe to call from any admin shell render," which an impure render-phase function cannot guarantee under concurrent rendering. Was an effect-based or router-event-based approach considered and rejected, or does the accordion-collapse-on-revisit behavior specifically need the value during first render?
- **`toJsonSafeContext` in `persist-app-log.ts`** — it performs a JSON round-trip, discards the result, and returns the original value, so it works as a serializability probe but does not sanitize. Was returning the un-normalized value intentional (letting Supabase serialize), or was the intent to return the round-tripped clone so nested `Error` and `Date` values normalize before insert?
- **Test placement convention across admin surfaces** — logs tests its server actions at the barrel (`logs/actions.unit.test.ts`, 451 LOC covering both `_lib` action modules) while users tests at the module (`users/_lib/list-actions.unit.test.ts`). Coverage is comparable either way. Is one the intended convention worth stating in `testing.mdc`, or is placement deliberately left to the author?
- **`cliLog` vs `appLog` threshold caching** — `cliLog` caches the log-level threshold for the process lifetime while `appLog` re-resolves per call. Fine for short-lived CLI processes, but is the divergence deliberate or incidental?
- **`(app)/layout.tsx` passing `children` to both the Suspense fallback and the resolved shell** — does the page subtree render twice per request while the banner resolves, or does React Flight's reference deduplication make it free? If it renders twice, any page-level fetch not wrapped in `cache()` runs twice.

## Resolved

| ID | Resolved | Notes |
| ---- | -------- | ----- |
| F118 | 2026-08-29 | `useRefreshIndicator` in `src/hooks/` owns the in-flight counter, 1000ms hold, and unmount clear; logs / users / `/reference` call it; `REFRESH_MIN_VISIBLE_MS` lives next to the hook; `AdminRefreshButton` in `admin/_components/` is the toolbar icon-button on both admin toolbars. Filtered-empty-state Refresh text buttons and the `/reference` toolbar icon were deliberately left — different controls, under the extract threshold. F167 / F183 / F174 / F158 were not done here. |
| F155 | 2026-08-29 | Closed via F118 — shared `useRefreshIndicator` clears the hold timer and resolves the pending promise on unmount; colocated unit test covers concurrent refreshes and the minimum-visible hold. |
| F177 | 2026-08-29 | Closed via F118 — `AdminRefreshButton` in `admin/_components/` replaces the duplicated toolbar icon-button on both admin toolbars. |
| F152 | 2026-08-29 | Shared `MIN_PASSWORD_LENGTH`; password submit awaits `referenceDemoMockPersist` so "Updating…" can appear; blob avatar URLs revoke on unmount; demo envelopes use `CONFLICT` / `INTERNAL_ERROR`; `app-toast.ts` owns info / warning / error / promise helpers and the toast section calls those; header comment lists only remaining intentional divergences (absent first-password branch). `reference/_components/**` stay coverage-excluded by design — the parity claim is what this closed, not the exclusion. F118 / F155 / F177 were not done here. |
| F117 | 2026-08-29 | `useActiveAnchor` in `src/hooks/` owns the observer, sort, and disconnect; both section navs call it; `SECTION_SCROLL_CLASS` in `src/constants/section-scroll.ts` is the only `scroll-mt-24` name; the two page-prefixed aliases are gone. Shared pill markup and the `border-b` wrapper drift were deliberately left — two sites is under the extraction threshold, so no shared `SectionNav` component was extracted. F152 / F118 / F155 were not done here. |
| F163 | 2026-08-29 | `proofCta.links` use `REFERENCE_PATH` and `WORKFLOW_PATH`. Unused import survived because `noUnusedLocals` is off and there is no unused-imports ESLint rule — not added here. F117 / F152 were not done here. |
| F171 | 2026-08-29 | Lucide `Map` / `Table` imported as `MapIcon` / `TableIcon` and restored to alphabetical order. F117 / F152 were not done here. |
| F173 | 2026-08-29 | Fallback maps `referenceShipmentsColumns` for id, skeleton, alignment, and `headerTitle`; live headers read the same meta field. Optional `headerTitle` added to shared `ColumnMeta`. Users-table fallback not migrated. F117 / F152 / F118 were not done here. |
| F150 | 2026-08-29 | `scripts/admin/lib/env.ts` is gone; `cli.ts` imports `loadServiceEnvForCli` from `@/utils/env`; `admin-users` re-exports only `deleteUserById`; `isUserAdmin` alias is gone. F174 / F185 were not done here. |
| F172 | 2026-08-29 | `use-mobile` has `'use client'` and reads `matches`. F174 / F185 were not done here. |
| F184 | 2026-08-29 | Avatar-upload options use `RefObject`. F174 / F185 were not done here. |
| F145 | 2026-08-29 | Unused profile converters and `buildUserListFilterLabels` plus their test blocks are gone; `ErrorPanel` calls `buildStructuredCopyText` directly; `hasBannerPreviewContent` is file-private; `buildBannerDismissKey` is the only public name; `getSupabaseUrlOptional` is no longer exported; sitemap helper is named-export only. F150 / F172 / F184 were not done here. |
| F164 | 2026-08-29 | `ReactQueryProvider` is a named export in `react-query-provider.tsx`. F150 / F172 / F184 were not done here. |
| F165 | 2026-08-29 | `env-var-warning` uses alias imports and plain string attributes. F150 / F172 / F184 were not done here. |
| F148 | 2026-08-29 | `mergeDemoteMetadata` takes no arguments; `PublicBannerSlot.initialDismissed` and its unreachable-branch test are gone; demote call site updated. F158 / F150 / F145 / F164 were not done here. |
| F115 | 2026-08-29 | Copy-to-clipboard holds and clears the timeout on reschedule and unmount; `TRANSIENT_SUCCESS_MS` shared with `field-save-indicator`; colocated hook unit test added. F158 / F150 / F145 / F164 were not done here. |
| F168 | 2026-08-29 | Failed session probe logs `appLog.warn` with tag `probe-session` and `{ supabaseCode }`; return shape unchanged; probe file added to the `appLog` allowlist. F158 / F150 / F145 / F164 were not done here. |
| F151 | 2026-08-29 | CLI factory deleted; `cli.ts` imports `createServiceClient` from `@/supabase/service`; auth options have one home; shared factory signature unchanged (no optional env). `AdminEnv` in `scripts/admin/lib/env.ts` now has no importer — `service-client.ts` was its only one — which is F150 territory and not fixed here. F148 / F150 were not done here. |
| F149 | 2026-08-29 | Each `run-*` file folded into its `'use server'` action file; four files became two; actions import `AdminUserMutationActionResult`; one exported result type per family; aliases deleted; `AdminUserTargetInput` lives on `run-admin-user-mutation.ts`, both families import it, and the barrel re-exports it. Family result types intentionally carry the full family status union — `RoleMutationActionResult` admits all four role statuses, so `promoteUserAction` types as able to return `demoted` / `not_admin` even though `Exclude<PromoteUserByIdResult['status'], 'not_found'>` narrows the call itself; this is the pre-existing shape (the aliases were already this wide) and matches what `getRoleMutationToastMessage` consumes. F148 / F151 were not done here. |
| F179 | 2026-08-29 | One exported result type per mutation family; per-action aliases deleted; `AdminUserTargetInput` replaces `RoleMutationActionInput`; barrel re-exports the neutral input type once. Bundled with F149. F148 / F151 were not done here. |
| F176 | 2026-08-29 | `useResetOnChange` in `src/hooks/` owns the render-phase previous-value track; banner settings section, users table state, and logs table state call it. F118 / F177 / F149 were not done here. |
| F178 | 2026-08-29 | Both table-state hooks return `listError`; logs table wires the error panel from that name. F118 / F177 / F149 were not done here. |
| F116 | 2026-08-29 | Parent-walk and rAF tween replaced with deferred `scrollIntoView({ block: 'start' })`; `matchMedia('(prefers-reduced-motion: reduce)')` picks `behavior`; public return unchanged; 200ms deferral keyed to `--animate-accordion-down` ships behind a `// debt:` marker because synchronous `scrollIntoView` lands short before expand layout; native smooth scroll kept (tween not restored); position and duration judged in the app-shell profile dialog (desktop + narrow); page behind the overlay did not move. Marker harvested as F193 this sync. F118 and F159 were not done here. |
| F133 | 2026-08-29 | Colocated cursor-stack test covers advance, rewind, advance-after-rewind (truncate), reset-on-filter, and empty-page no-op. F176, F178, and F118 were not done here. |
| F134 | 2026-08-29 | Shell unit test covers the three body states plus stale-rows and Enter/Space activation; controlled/uncontrolled sorting branch left uncovered by decision (F135 closed it at the type level). F118 was not done here. |
| F136 | 2026-08-29 | `parseAppSettingValue` colocated test covers the three value-type branches plus unknown-key rejection; banner depth stays in the existing banner-schema test. F159 and F176 were not done here. |
| F188 | 2026-08-29 | Homepage no longer sets a partial `openGraph` override — canonical kept; root file-convention image reaches the head. Root `getSiteMetadata` sets `openGraph.url: '/'` because dropping the page override removed the prior URL. F118 was not done here. |
| F190 | 2026-08-29 | `revalidateProfileDialogHosts()` in `_lib/profile/` lists `/(app)`, `/admin`, and `/(marketing)`; all three profile actions call it; host comment points at the helper for fourth-shell registration. F118 was not done here. |
| F166 | 2026-08-29 | Security-audit DB / RLS surface-map row says 19 migrations. F118 was not done here. |
| F132 | 2026-08-29 | `useAppSettingSave` in `settings/_lib/` owns saving / error / reset-on-`savedValue` and the save sequence; three rows keep schema plus fields and the call-site disabled guard; banner success uses the parsed value (no `as BannerSettingValue`); single-pass partition exports the grouped non-banner map and `BANNER_REGISTRY_ENTRIES`; panel and banner section consume those lists; banner heading reads `APP_SETTINGS_GROUP_BANNERS`; `BannerAppSettingRegistryEntry` lives on the registry. F136 / F159 / F176 were not done here. |
| F157 | 2026-08-29 | Single-pass partition in `settings/_lib/app-settings-partition.ts` exports `GROUPED_NON_BANNER_REGISTRY_ENTRIES` and `BANNER_REGISTRY_ENTRIES`; panel and banner section consume those lists; banner heading reads `APP_SETTINGS_GROUP_BANNERS`. Bundled with F132. F136 / F159 / F176 were not done here. |
| F129 | 2026-08-29 | `NonBannerAppSettingRegistryEntry` on the registry; `AppSettingRow` takes `savedSettings` and dispatches `log_level` / `positive_int` / `never`; six assertions gone; F062 `// debt:` marker deleted because the compiler is the gate. Panel filter narrows via TS 5.9 inference (no hand-written predicate). F132, F157, and F136 were not done here. |
| F180 | 2026-08-29 | Private `withResolvedUser` in `admin-user-mutations.ts` owns fetch, throw, `not_found`, and coalesces `email` to `string | null`; five mutations are distinct-branch only. Eight `user.email!` in that file plus the `listAdminUsers` assertion in `scripts/admin/lib/admin-users.ts` are gone. Runner envelope, `isMutationFound` predicate (renamed from `hasMutationEmail`), and both action success types widened to `email: string | null`; success log uses `result.email ?? userId`. F149, F179, and F148 were not done here. |
| F162 | 2026-08-29 | Result types' success arms and the admin mutation envelope now declare `email: string | null`; coalesce once in `withResolvedUser` (F180). Unit test: `promoteUserById` with `email: undefined` returns `email: null` and still promotes. |
| F186 | 2026-08-29 | `compareWiredChecks(scripts, workflowText)` exported from `checks-wired.mjs`; colocated test covers the `check:a11y` / `check:a11y-contrast` prefix collision; `checkPnpmOnly` exported from `pnpm-only.mjs` with lockfile and `packageManager` cases; both wrapped in guarded `isMain`; both `// debt:` excludes deleted from `vitest.config.ts`; `scripts/**` floor reset from fresh `test:ci` (`Math.floor`, no cushion) to 70 / 75 / 82 / 70 (lines / functions / branches / statements). F174 and the rest of the enforcement-layer tests were not done here. |
| F119 | 2026-08-29 | Shared traversal in `eslint-rules/lib/class-string-visitor.mjs`; both rules call `createClassStringVisitor` with their own leaf scanner; `isCnImportSource` collapsed from three redundant clauses to one `endsWith` test; existing `lintText` pins unchanged; `eslint-rules/**` floor reset from fresh `test:ci` (`Math.floor`, no cushion) to 71 / 62 / 60 / 71 (lines / functions / branches / statements). F174, F186, and extra walker `lintText` cases were not done here. |
| F109 | 2026-08-29 | One import direction: types module keeps primitives (`LOG_LEVELS`, `LogLevel`, `AppSettingValueType`, `AppSettingValueByType`); registry-derived types (`AppSettingKey`, `AppSettingValueMap`, entry types, `ResolvedAppSettings`) imported from the registry; types re-export block deleted; registry `LOG_LEVELS` re-export deleted; `madge --circular --ts-config` clean. Retained: `app-logs.ts` `LogLevel` re-export (logs-domain convenience, not part of the cycle); `ResolvedAppSettings` name on the registry rather than collapsing into `AppSettingValueMap`. F129 and F132 were not done here. |
| F170 | 2026-08-29 | Consumer confirmed — `AdminAuthGate` mounts `ProfileDialogHost`, whose `ProfileDialogTree` reads `hasPassword` server-side to choose Set Password vs Change Password. The audit looked for an admin surface showing another user's flag; the real consumer is the signed-in admin's own dialog, mounted on the admin shell by the F061 hoist. Keep both revalidate calls, no code change. Note the finding's stated mechanism was wrong: no server-side cache holds the flag today — `getCurrentUserProfile` is React `cache()` (per-request), and with `cacheComponents: true` and no `'use cache'` on this path the admin tree is a dynamic hole rendered per request. The calls act on the client Router Cache. Kept path-specific rather than collapsed to `revalidatePath('/', 'layout')` because the current cross-path refresh side effect is documented as temporary and will narrow to the named path. Route-group paths are a supported argument form, so `'/(app)'` resolves. The marketing shell also mounts the dialog and is revalidated by nothing — filed as F190. |
| F128 | 2026-08-29 | Zod `safeParse` at the top of `listUsersAction` after `assertAdminCaller`; `emailFilter` is a string with max 200 (same cap as logs search); non-string no longer throws as `INTERNAL_ERROR`; hand-rolled checks deleted. F149, F179, and F180 were not done here. |
| F147 | 2026-08-29 | PM decision: tint Unread. Logs tile now uses the `unread` role (primary). `total`/`debug` selected borders collapsed into one compound entry. |
| F161 | 2026-08-29 | `noUncheckedIndexedAccess` on; `exactOptionalPropertyTypes` still off. Eleven TypeScript files guarded (JWT payload index, regex groups, observer first-entry, `Promise.all` spread tail, landing 2-tuple, fixture pick). Check-script regex match groups describe real code but live in `.mjs` outside the TypeScript compile — this flag does not reach them; `checkJs` was considered and declined. |
| F061 | 2026-08-29 | One `ProfileDialogHost` per kept shell (app layout, marketing layout, admin gate); chips no longer wrap a provider; admin sidebar included; streaming split preserved (sync host, async tree). |
| F105 | 2026-08-29 | Helper and envelope moved to `admin/_lib/assert-admin-caller.ts`; envelope renamed `AdminActionError`; logs shim deleted; `map-admin-action-fault` imports sibling, not upward from users; `security.mdc` and LEXICON paths updated. F149, F179, and F128 remain Deferred / Do next and were not done here. F128 closed separately 2026-08-29. |
| F106 | 2026-08-29 | Helper parameterized over success `TStatus` as a genuine discriminated union; four `run-*` callers pass explicit `Exclude<*ByIdResult['status'], 'not_found'>` type arguments; action success fields use `RoleMutationSuccessStatus` / `BanMutationSuccessStatus`; both hook casts deleted. Generic `hasMutationEmail` predicate retained — inline `status === 'not_found'` does not narrow under unconstrained `TStatus extends string`. F149 and F179 remain Deferred and can now import `AdminUserMutationActionResult` instead of redeclaring the envelope. |
| F089 | 2026-08-29 | `useMounted` in `src/hooks/` carries the single suppression; theme menu, profile segment, and banner row converted; logs localStorage restore left alone. |
| F189 | 2026-08-29 | Per-glob coverage floors in `vitest.config.ts` — initial `eslint-rules/**` baseline 48 / 53 / 57 / 48 (lines / functions / branches / statements); re-baselined 2026-08-29 after F119 to 71 / 62 / 60 / 71; initial `scripts/**` baseline 74 / 77 / 83 / 74; re-baselined 2026-08-29 after F186 to 70 / 75 / 82 / 70. Measured with no cushion (`Math.floor` on fresh `test:ci` aggregate). F186 files (`checks-wired.mjs`, `pnpm-only.mjs`) were excluded until F186 closed this date. Any further denominator change in those globs requires re-baselining the floor rather than assuming headroom. |
| F121 | 2026-08-29 | Moved `supabase-env.mjs` to `scripts/build/` with `[build:supabase-env]` prefix and a build-only header comment; still invoked only from `pnpm build`, not a `check:*` gate. |
| F110 | 2026-08-29 | Audit skill stack-tooling madge lines now match § Tooling notes, including `--extensions ts,tsx --ts-config tsconfig.json src`. |
| F182 | 2026-08-29 | Removed unused `registries` block from `components.json`; deleted the shadcnblocks example from `ui-shadcn.mdc` and added a third-party-registry review expectation in the same change. `baseColor` retained — the shadcn `components.json` schema requires it, so removing it would break `shadcn add`. Removal is config hygiene, not an enforcement fix: the CLI accepts a bare registry URL, so vendoring into `src/components/ui` remains ungated by design — the `ui-shadcn.mdc` line covers it. |
| F120 | 2026-08-29 | Three named ESLint hard-constraint gates now use per-check flat-config exports (`eslint.gate-*.mjs` via `namedGate`) with `--config` and `--no-config-lookup`, not `--rule` merge. Each gate keeps its own cache directory. The shadcn gate scopes to the `seminova/no-shadcn-pkg` `no-restricted-imports` block only, so a server-only import does not fail it. AGENTS.md Enforced parentheticals updated in the same change. |
| F108 | 2026-08-28 | All four custom lint rules now have `lintText` pins in `eslint.config.unit.test.ts`; helper resolves the rule from whichever plugin owns it. |
| F122 | 2026-08-28 | Coverage `include` now covers `scripts/**` and `eslint-rules/**` so the enforcement layer appears in the report; untested admin CLI shims and `vitest-file.mjs` are explicit commented excludes (the two F186 checkers were excluded until F186 closed 2026-08-29). Measured and visible, not closed — the global threshold still does not gate this code; F189 adds per-glob thresholds. |
| F138 | 2026-08-28 | Five check scripts (`a11y-structure`, `a11y-contrast`, `seo-base-url`, `no-profiles-role`, `supabase-env`) now print every collected violation with `console.error`, then call `fail` once with a count summary. Original four plus `supabase-env` (same shape; still build-only / F121). |
| F103 | 2026-08-28 | Relay strips caller-supplied `userId` from capped context before merge; server-read session is the only attribution path. Integration test asserts the unauthenticated forge is dropped. Commit eab320d. |
| F104 | 2026-08-28 | `redirectWithAuthCookies` copies the full cookie object so `HttpOnly`, `Secure`, `SameSite`, and expiry survive login and stray-code redirects. Proxy tests assert `HttpOnly` on the emitted `Set-Cookie`. Commit eab320d. |
| F107 | 2026-08-28 | `admin_list_users` returns `LIMIT per_page + 1`; the list helper slices the overflow row and derives `hasNextPage` from it. Unit tests cover the exact-full-page boundary. Migration `20260828193941_admin_list_users_per_page_plus_one_probe.sql` — human `pnpm db:push` still required if not applied. Commit eab320d. |
| F114 | 2026-08-28 | Sign-up and update-password forms now read submitted values from FormData, enforce `MIN_PASSWORD_LENGTH` client-side, and sign-up password fields carry `name` attributes for manager autofill. |
| F127 | 2026-08-28 | `/auth/confirm` validates OTP `type` via zod enum before `verifyOtp`; invalid types fall through to `invalid_link`. |
| F169 | 2026-08-28 | Update-password form logs email lookup failures, omits the hidden username field on confirmed failure, and shows a muted status note; `forms.mdc` carve-out added. |
| F112 | 2026-08-28 | Deleted unused `LogoutButton` component and its integration test; sign-out remains via `useSignOut` only. |
| F113 | 2026-08-28 | Removed inert unmount `useLayoutEffect` cleanup from `EmailOtpRequestCard`; removed two integration tests that asserted hide/show reset via that effect (not a production path — consumers are separate routes). |
| F135 | 2026-08-28 | Split `useDataTableShell` options into controlled/uncontrolled union; non-null assertion on `onSortingChange` deleted. |
| F160 | 2026-08-28 | Test-harness `QueryClient` now constructed inside `Wrapper` via `useState` so each top-level render gets an isolated cache. |
| F139 | 2026-08-28 | Deleted stale site-header comment referencing dissolved footer grid pairing (F060). |
| F141 | 2026-08-28 | Deleted stale pre-push-only marker from `no-profiles-role.mjs`; CI runs `check:admin-gate`. |
| F142 | 2026-08-28 | Reworded `app-setting-row.tsx` marker to name a third **non-banner** `valueType` as the refactor trigger. |
| F143 | 2026-08-28 | Hoisted admin dashboard card hover/tier class bundle to module constant; fifth F144-class site (identifier `className`, lint does not follow it). |
| F111 | 2026-08-28 | Restored the missing lightness-and-alpha segment on the `.dark` `--shadow` token's second layer so it matches `:root`; cards regain correct elevation in dark mode. |
| F137 | 2026-08-28 | Auth layout inner wrapper is now `<main id="main-content">`, matching other route groups for screen-reader landmark navigation. |
| F146 | 2026-08-28 | Deleted unused shadow-composition tokens (`--shadow-x` through `--shadow-color`) and `--tracking-normal` from `globals.css`; longhand `--shadow-*` values unchanged. |
| F156 | 2026-08-28 | Settings panel rows keyed by `entry.key` only; successful save no longer remounts the row and discards focus. |
| F187 | 2026-08-28 | Auth-boundary matcher gaps closed with S012. Matcher permits 5–6 char route-group hash suffixes at the metadata-image position only; `/auth/**` is segment-bound; `isPublicRoute` exported; metadata-image file location discovery added; `check:auth-boundary` widened to matcher tests. Homepage `og:image` head-tag gap split to F188. |
| F140 | 2026-08-28 | Stale `// debt:` marker deleted from `proxy.unit.test.ts` — `check:auth-boundary` is a named pre-push and CI step. Closed with S012. |
| F102 | 2026-08-28 | Five stale override floors raised, `nanoid: '>=3.3.18'` added, `postcss` devDependency raised 8.5.10 → 8.5.23 (resolves 8.5.26 via the override floor). `pnpm audit`: zero. **This finding's recommendation was wrong as written and caused two failed attempts.** It prescribed "prefer range pins (`>=x.y.z`) over exact so future patches float" — correct for a single-major override, wrong for a major-scoped one. `brace-expansion@1: '>=1.1.18'` does not mean "1.x, patched"; the resolver satisfied it with a 5.x release, which is what left a stale 5.0.7 in the tree and why the first attempt still audited dirty. Collapsing to one unscoped `brace-expansion: '>=5.0.9'` then closed the advisory but broke `pnpm lint` — ESLint's `minimatch@3.1.5` is a 1.x-API client and 5.x does not export the callable shape (`expand is not a function`). The form that holds is **selector-scoped with a caret cap**: `@1: '^1.1.18'`, `@2: '^2.1.4'`, `@5: '^5.0.9'`. `pnpm why brace-expansion` confirms three distinct copies, each on its own major at its patched floor. The lesson is recorded as a comment in `pnpm-workspace.yaml` itself, since that file is what the next editor reads. Commit 820846d. |
| F081 | 2026-08-28 | `sharp` override and its comment block removed in the same commit as F102. Next.js 16.3.3 declares optional `sharp@^0.35.3` itself, so the override was redundant rather than load-bearing; post-removal `sharp` still resolves at 0.35.3 through Next. |
| F101 | 2026-08-28 | `next` upgraded 16.2.9 → **16.3.3**, not the ≥16.2.11 this finding specified. The target moved between the audit and the fix: the July 2026 security release made 16.2.11 Active LTS, but the August 25 release advanced Active LTS to 16.3 and patched two Critical advisories in 16.3.3 / 15.5.24 only — 16.2.11 does not receive them. Lockfile diff was `next`, `@next/swc-*`, `@next/env`, and `@swc/helpers` 0.5.15 → 0.5.23; nothing else moved. `CI=true pnpm pre-push` green (781 tests), no type break across two minors. Auth boundary verified manually: unauthenticated `/home` and `/admin/settings` both 307 to `/auth/login`. Branch `chore/upgrade-next-16.3.3`. Two consequences filed separately — F081's blocker cleared, and manual smoke surfaced F187. **Lesson for future passes: confirm which release line is current Active LTS before recording a version target; a patch floor goes stale when the LTS line advances.** |
| F060 | 2026-08-28 | Resolved by dissolution rather than by fix — Phase 19 changed `site-footer.tsx` to `md:grid-cols-2`, so the duplicated `md:grid-cols-[minmax(0,1fr)_auto_minmax(0,1fr)]` track now has a single occurrence in `site-header.tsx`. No third surface ever appeared; the second one went away. The now-stale pairing comment that still references this ID is tracked as F139. |
| F126 | 2026-08-28 | Closed with S010. `getSecurityHeaders()` now emits Referrer-Policy, X-Content-Type-Options (nosniff), and a restrictive Permissions-Policy alongside CSP, X-Frame-Options, and HSTS; pinned in `security-headers.unit.test.ts`. |
| F130 | 2026-08-28 | Users list now passes the 3-character-gated `appliedSearch` as `emailFilter`, so typing 1–2 characters no longer creates a distinct query key or extra round-trip. |
| F131 | 2026-08-28 | Mark-read, unread, and mark-all invalidate list and stats only; the tags query stays cached until a new row or Refresh. |
| F154 | 2026-08-28 | Logged-out mobile header renders Sign in / Sign up directly; `hasServerAuthSession` is wrapped in React `cache` so remaining probes in the same request share one JWT verify. |

_(The 2026-07-21 and 2026-07-22 resolutions from the previous window — F011, F059, F063–F070, F073–F079, F083, F084, F086–F088, F090, F092–F094, F096–F099 — were pruned on this full pass per the skill's retention rule. Their detail lives in git history and the archived [Phase 14 PRD](docs/prds/archive/phase-14-tech-debt-hardening.prd.md).)_

## Tooling notes

| Tool | Result |
| ---- | ------ |
| `pnpm audit` | **No known vulnerabilities found.** (Was 20 — 14 high, 6 moderate — at the full pass; F101 closed the 9 `next` advisories, F102 the remaining 11.) |
| `pnpm type-check` | Pass |
| `pnpm lint` | Pass |
| `pnpm test:ci` | Pass — 868 tests / 167 files; coverage thresholds met (80% lines/functions/branches/statements) |
| `npx madge --circular --extensions ts,tsx --ts-config tsconfig.json src` | No circular dependency found |
| `npx madge --circular src` (no `--ts-config`) | Do not use — resolves no `@/*` aliases, so cycles that cross the alias boundary are invisible |
| `npx knip` | Unused exports dominated by the vendored shadcn sidebar/table/dropdown surface (expected, § Verified OK). Genuine hits folded into F145. `msw` still flagged unused (F072, accepted). |

**Note on `CI=true`:** local runs of `pnpm type-check` / `pnpm lint` / `pnpm test:ci` abort with `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY` in a non-TTY shell. Prefix with `CI=true` when running gates from an agent or script. Not a finding — worth knowing.

Adapted from [ksimback/tech-debt-skill](https://github.com/ksimback/tech-debt-skill) (MIT).
