/**
 * Mirror of Supabase Dashboard → Authentication → Providers → Email
 * (dashboard is authoritative).
 */
export const AUTH_OTP_CODE_LENGTH = 6 as const

export const AUTH_OTP_LIFETIME_MINUTES = 15 as const

export const AUTH_OTP_MIN_SEND_INTERVAL_SECONDS = 30 as const

/**
 * Mirror of Supabase Dashboard → Authentication → Settings → Password Requirements
 * (dashboard is authoritative).
 */
export const MIN_PASSWORD_LENGTH = 8 as const
