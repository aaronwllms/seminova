export const WorkflowConventionsSection = () => (
  <section aria-labelledby="agent-conventions" className="border-t pt-10">
    <h2
      className="text-2xl font-semibold tracking-tight"
      id="agent-conventions"
    >
      Agent-ready conventions
    </h2>
    <div className="text-muted-foreground mt-4 max-w-prose space-y-4 text-[15px] leading-relaxed">
      <p>
        The second differentiator is how the template encodes quality bars so
        any AI agent inherits the same standards on day one. Coding rules,
        architectural vocabulary, and invokable skills are part of the product —
        not documentation you read once and forget.
      </p>
      <p>
        Hard constraints are mechanically enforced: auth boundaries, admin
        gates, semantic theming, and SEO base URLs fail CI when violated. Agents
        read repo truth before they code and sync docs after behavior changes so
        the next session starts from an accurate picture.
      </p>
      <p>
        Spinoffs keep the structure and replace the theme. The workflow,
        document roles, and agent conventions travel with the template; product
        identity and domain vocabulary are what you customize.
      </p>
    </div>
  </section>
)
